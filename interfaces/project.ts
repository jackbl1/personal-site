type ProjectType = {
  slug: string;
  title: string;
  link: string;
  date: string;
  coverImage: string;
  excerpt: string;
  ogImage: {
    url: string;
  };
  content: string;
};

export default ProjectType;
